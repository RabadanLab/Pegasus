/*
 * $Id: unseteval.nsql 4432 2011-10-01 04:07:19Z unsaved $
 *
 * Invokes another script that defines variables.
 * Then we test that we have access to the variables.
 */

*if (*{unset} == 3)
    \p Should not succeed
*end if
